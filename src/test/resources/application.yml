spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/blog_test
    username: root
    password: root

  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: validate